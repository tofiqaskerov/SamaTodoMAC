{
  "name": "sama-todo",
  "version": "1.0.0",
  "description": "Lofi background To-Do desktop app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . SamaTodo --platform=darwin --arch=x64 --overwrite --out=dist --icon=icon_sama.icns"
  },
  "devDependencies": {
    "electron": "^39.2.2",
    "electron-packager": "^17.1.2",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
